
export const alphaQuery = async () => ({ name: "alpha", value: Math.random() })
export const betaQuery = async () => ({ name: "beta", value: Math.random() })
export const centauriQuery = async () => ({ name: "centauri", value: Math.random() })
